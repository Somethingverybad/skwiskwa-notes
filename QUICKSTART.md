# 🚀 Быстрый старт

## Самый простой способ (Mac/Linux)

```bash
cd notion-clone
./start.sh
```

## Самый простой способ (Windows)

```cmd
cd notion-clone
start.bat
```

## Или запустить вручную

### 1. Backend (Терминал 1)

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r ../requirements.txt
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
```

### 2. Frontend (Терминал 2)

```bash
cd frontend
npm install
npm run dev
```

### 3. Откройте браузер

http://localhost:3000

## 🎯 Основные возможности

### Создание страниц
- Нажмите "+ Новая страница" в сайдбаре
- Введите название страницы

### Работа с блоками
- Нажмите "+ Добавить блок" для выбора типа блока
- Доступны: текст, заголовки (H1, H2, H3), чекбоксы, цитаты, разделители
- Медиа: изображения, видео, аудио, файлы

### Drag & Drop
- **Перемещение блоков**: Наведите на блок → появится иконка ⋮⋮ → перетащите
- **Загрузка файлов**: Перетащите файл в зону загрузки медиа-блока

### Автосохранение
Все изменения автоматически сохраняются при:
- Потере фокуса с текстового поля
- Изменении чекбокса
- Загрузке файла

## 📝 Примеры использования

### Создание документации
1. Создайте страницу "Документация"
2. Добавьте H1 заголовок
3. Добавьте текстовые блоки с описанием
4. Вставьте изображения через drag & drop

### ToDo список
1. Создайте страницу "Задачи"
2. Добавляйте блоки типа "Чекбокс"
3. Отмечайте выполненные задачи

### База знаний с медиа
1. Создайте страницу
2. Добавьте видео-блок
3. Перетащите видео файл
4. Добавьте описание текстовыми блоками

## 🛠️ Разработка

### Backend API
- Swagger UI: http://localhost:8000/api/ (автоматическая документация)
- Admin панель: http://localhost:8000/admin/

### Структура базы данных
- **Page** - страницы (title, icon, cover_image)
- **Block** - блоки контента (block_type, content, file, order)
- **Comment** - комментарии к блокам

## 🐛 Устранение проблем

### Backend не запускается
```bash
cd backend
source venv/bin/activate
pip install -r ../requirements.txt --force-reinstall
python manage.py migrate
```

### Frontend не запускается
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### Порт уже занят
Измените порт в `backend/notion_clone/settings.py` или `frontend/vite.config.ts`

## 📚 Дополнительно

- Полная документация: `README.md`
- API Reference: http://localhost:8000/api/
- GitHub Issues: для вопросов и предложений
