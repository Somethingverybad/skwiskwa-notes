# 🔐 Настройка аутентификации

## ✅ Что было добавлено

### Backend:
- ✅ JWT аутентификация (djangorestframework-simplejwt)
- ✅ Endpoints для регистрации и входа
- ✅ Связь пользователей со страницами
- ✅ Защита API endpoints (только авторизованные пользователи)
- ✅ Автоматическое обновление токенов

### Frontend:
- ✅ Компоненты Login и Register
- ✅ Контекст аутентификации (AuthContext)
- ✅ Защита маршрутов (ProtectedRoute)
- ✅ Автоматическое добавление токена к запросам
- ✅ Автоматическое обновление токена при истечении

## 🚀 Применение изменений

### 1. Обновить зависимости

**В Docker:**
```bash
docker-compose exec backend pip install -r /app/requirements.txt
```

**Локально:**
```bash
cd backend
pip install -r ../requirements.txt
```

### 2. Применить миграции

**В Docker:**
```bash
docker-compose exec backend python manage.py makemigrations
docker-compose exec backend python manage.py migrate
```

**Локально:**
```bash
cd backend
python manage.py makemigrations
python manage.py migrate
```

### 3. Установить frontend зависимости

**В Docker:**
```bash
docker-compose exec frontend npm install
```

**Локально:**
```bash
cd frontend
npm install
```

### 4. Перезапустить контейнеры

```bash
docker-compose restart backend frontend
```

## 📋 API Endpoints

### Аутентификация

- `POST /api/auth/register/` - Регистрация
  ```json
  {
    "username": "user123",
    "email": "user@example.com",
    "password": "securepass123",
    "password2": "securepass123"
  }
  ```

- `POST /api/auth/login/` - Вход
  ```json
  {
    "username": "user123",
    "password": "securepass123"
  }
  ```

- `POST /api/auth/logout/` - Выход
  ```json
  {
    "refresh": "refresh_token_here"
  }
  ```

- `GET /api/auth/me/` - Информация о текущем пользователе
  - Требует: `Authorization: Bearer <access_token>`

- `POST /api/auth/token/refresh/` - Обновление токена
  ```json
  {
    "refresh": "refresh_token_here"
  }
  ```

## 🔒 Защита данных

- Все страницы теперь привязаны к пользователю
- Пользователи видят только свои страницы
- Блоки доступны только со страниц пользователя
- Все API endpoints требуют аутентификации

## 🎯 Использование

1. **Регистрация:**
   - Откройте http://localhost:3000/register
   - Заполните форму
   - После регистрации автоматически войдете в систему

2. **Вход:**
   - Откройте http://localhost:3000/login
   - Введите username и password
   - После входа перенаправит на главную страницу

3. **Работа:**
   - Все страницы автоматически привязываются к вашему аккаунту
   - Только вы можете видеть и редактировать свои страницы

## 🐛 Устранение проблем

### Ошибка "Authentication credentials were not provided"

**Причина:** Токен не отправляется или истек

**Решение:**
- Проверьте, что токен сохраняется в localStorage
- Выйдите и войдите снова
- Проверьте консоль браузера на ошибки

### Ошибка миграций

**Причина:** База данных не обновлена

**Решение:**
```bash
docker-compose exec backend python manage.py makemigrations content
docker-compose exec backend python manage.py migrate
```

### Страницы не загружаются

**Причина:** Пользователь не авторизован или нет страниц

**Решение:**
- Убедитесь, что вы залогинены
- Создайте первую страницу через кнопку "Новая страница"

## 📝 Примечания

- Токены хранятся в localStorage браузера
- Access token действителен 1 день
- Refresh token действителен 7 дней
- При истечении access token автоматически обновляется через refresh token
- При выходе refresh token добавляется в blacklist

## 🔄 Миграция существующих данных

Если у вас уже есть страницы без владельца:

```python
# В Django shell
from content.models import Page
from django.contrib.auth.models import User

# Назначить все страницы без владельца первому пользователю
user = User.objects.first()
Page.objects.filter(owner__isnull=True).update(owner=user)
```

Или через admin панель:
1. Откройте http://localhost:8000/admin/
2. Перейдите в Pages
3. Выберите страницы без владельца
4. Измените owner вручную

---

**Готово!** 🎉 Аутентификация настроена и работает!
